export const goToPokedex = (history) => {
    history.push('/')
}

export const goToDetailPage = (history) => {
    history.push('/detail')
}

export const goToHome = (history) => {
    history.push('/')
}